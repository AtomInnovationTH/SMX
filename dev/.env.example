# Required by art-gen/gen.py (image generation via OpenRouter). Never commit the real .env.
OPENROUTER_API_KEY=sk-or-v1-...
# Optional: overrides the default model.
ART_MODEL=google/gemini-3-pro-image
